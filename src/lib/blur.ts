// Kept in sync with the .balances-blurred selector list in index.css —
// tapping any one of these while a screen's amounts are blurred reveals
// all of them for the rest of that visit (see each view's own onClick).
export const BLURRABLE_SELECTOR =
  '.net-worth-amount, .net-worth-breakdown-amount, .total-chip .amount, .entry-amount, .dashboard-card-total, .dashboard-networth-line, .dashboard-amount'
